# Docker Compose 本地开发配置示例。
# 复制为 .env.docker.local 后再修改；不要提交真实密码或生产密钥。

SPRING_DATASOURCE_URL=jdbc:mysql://host.docker.internal:3306/mall?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
DB_USERNAME=root
DB_PASSWORD=change_me

# 仅用于本地开发。部署到公网前请替换为至少 32 字节随机值的 Base64 编码。
JWT_SECRET=bWFsbC1sb2NhbC1kZXYtand0LXNlY3JldC0zMi1ieXRl

REDIS_HOST=redis
RABBITMQ_HOST=rabbitmq
RABBITMQ_USERNAME=mall
RABBITMQ_PASSWORD=mall
